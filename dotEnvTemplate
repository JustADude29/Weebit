PORT = 5000
NODE_ENV = development
ORIGIN = http://localhost:3000

JWT_SECRET = <secret>

DB_DIALECT = <database> (ex: postgres, mySQL)
DB_PORT = <port> (ex: 5432)
DB_HOST = <host> (ex: localhost)
DB_USERNAME = <username> 
DB_PASSWORD = <password>
DB_DATABASE = <database_name>